import React from 'react';
import {
    View,
    Text,
    ImageBackground,
    StyleSheet,
} from 'react-native';

const Card = (props) => {
    return(
        <ImageBackground>

        </ImageBackground>
    )
}
export default Card;