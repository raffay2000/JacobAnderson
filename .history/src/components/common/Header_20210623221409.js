import React from 'react';
import {
    View,
    Text,
    StyleSheet,
} from 'react-native';

export const Header = (props) => {
    return(
        <View>
            <Text>Header</Text>
        </View>
    )
}

const styles = StyleSheet.create({

})